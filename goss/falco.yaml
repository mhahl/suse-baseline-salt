# Goss tests for monitoring.falco

package:
  falco:
    installed: true
    title: "falco package is installed"

file:
  /etc/falco/falco.yaml:
    exists: true
    mode: "0644"
    owner: root
    group: root
    contains:
      - "http_output:"
      - "enabled: true"
    title: "falco HTTP output is enabled in config"
