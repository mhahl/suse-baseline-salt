# Goss tests for baseline.profile

file:
  /etc/profile.d/99-baseline.sh:
    exists: true
    mode: "0644"
    owner: root
    group: root
    contains:
      - "HISTTIMEFORMAT"
      - "PROMPT_COMMAND"
      - "TMOUT"
      - "umask 027"
      - "ulimit -S -c 0"