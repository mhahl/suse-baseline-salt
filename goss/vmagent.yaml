# Goss tests for monitoring.vmagent

user:
  vmagent:
    exists: true
    home: /var/lib/vmagent
    title: "vmagent system user exists"

file:
  /usr/sbin/vmagent:
    exists: true
    mode: "0755"
    owner: root
    group: root
    title: "vmagent binary is installed"

  /etc/vmagent/scrape.yml:
    exists: true
    mode: "0644"
    owner: vmagent
    group: vmagent
    contains:
      - "job_name:"
    title: "vmagent scrape config exists"

  /etc/systemd/system/vmagent.service:
    exists: true
    mode: "0644"
    owner: root
    group: root
    contains:
      - "vmagent"
      - "remoteWrite.url"
      - "remoteWrite.tmpDataPath"
    title: "vmagent systemd unit file exists"

  /var/lib/vmagent/vmagent-remotewrite-data:
    exists: true
    filetype: directory
    owner: vmagent
    group: vmagent
    title: "vmagent remote write data directory exists with correct ownership"

service:
  vmagent:
    enabled: true
    running: true
    title: "vmagent service is enabled and running"
