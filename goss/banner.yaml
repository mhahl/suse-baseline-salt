# Goss tests for baseline.banner

file:
  /etc/motd.d/99-steggy:
    exists: true
    mode: "0644"
    owner: root
    contains:
      - "It's a UNIX System!"
      - "I know this!"
    title: "Baseline motd banner is installed"
