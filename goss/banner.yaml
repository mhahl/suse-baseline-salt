# Goss tests for baseline.banner

file:
  /etc/motd.d/99-steggy:
    exists: true
    mode: "0644"
    owner: root
    contents:
      - "Authorized access only"
      - "Disconnect immediately if you are not an"
    title: "motd banner is installed"
