# Goss tests for baseline.integrity

package:
  rpm:
    installed: true

command:
  "rpm -q --quiet rpm":
    exit-status: 0

  "crontab -l -u root 2>/dev/null | grep -q 'rpm -Va' || true":
    exit-status: 0
