# Goss tests for baseline.fapolicyd (often disabled by default)

package:
  fapolicyd:
    installed: false   # Change to true if you enable the module

# When enabled in non-container:
# service:
#   fapolicyd:
#     enabled: true
#     running: false
#     skip:
#       - command: "systemd-detect-virt --container"
#         exit-status: 0
