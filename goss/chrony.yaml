# Goss tests for baseline.chrony

package:
  chrony:
    installed: true
    title: "chrony package is installed"

service:
  chronyd:
    enabled: true
    # In Goss container tests we intentionally do not start services
    running: false
    title: "chronyd service is enabled (not started in container tests)"

file:
  /etc/chrony.conf:
    exists: true
    mode: "0644"
    contains:
      - "server time1.google.com"
      - "rtcsync"
      - "minsources 3"
    title: "chrony config has hardened NTP servers"
